[build]
builder = "NIXPACKS"
buildCommand = "cd ../.. && npm install -g pnpm@latest serve && pnpm install && pnpm --filter @workspace/bruce3d run build"

[deploy]
startCommand = "serve -s dist/public -l $PORT"

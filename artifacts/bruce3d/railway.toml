[build]
builder = "NIXPACKS"
buildCommand = "cd ../.. && npm install -g pnpm@latest && pnpm install && pnpm --filter @workspace/bruce3d run build"

[deploy]
startCommand = "npx serve -s ./dist/public -l $PORT"
